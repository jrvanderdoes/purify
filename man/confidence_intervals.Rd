% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confidence_intervals.R
\name{confidence_intervals}
\alias{confidence_intervals}
\title{Bootstrapped Confidence Intervals}
\usage{
confidence_intervals(
  data,
  pred_model,
  h,
  train = 0.8,
  M = 1000,
  alpha = 0.05,
  output = NULL,
  ...
)
}
\arguments{
\item{data}{Data.frame (or vector) to apply cross-validation where the rows
are the observations and the columns are the variables.}

\item{pred_model}{Model to predict the data where the first argument in the
data and the second the length of prediction. Additional parameters can be
passed using ... . Returns a vector of the predictions.}

\item{h}{Number of observations to predict into the future.}

\item{train}{Minimum amount of data (0,1) to train model. After this point, a
sliding window approach is taken.}

\item{M}{Numeric. Number of bootstrap iterations.}

\item{alpha}{Significance level for the intervals}

\item{output}{Numeric or name of the column for the output when using a
data.frame for \code{data}. If not given, the first column is used.}

\item{...}{Additional parameters to pass to \code{pred_model}.}
}
\value{
Data.frame with columns for the forecasts and lower/upper confidence
intervals.
}
\description{
Method for the computation of bootstrapped confidence intervals.
}
\examples{
data <- cumsum(rnorm(150))
pred_model <- function(x, h) {
  predict(forecast::ets(x), h = h)$mean
}
h <- 10
ets_model <- predict(forecast::ets(data[1:140]), h)
ints <- confidence_intervals(data = data[1:140], pred_model = pred_model, h = h)

plot(ets_model)
lines(x = 141:150, y = ints$lower, col = "red")
lines(x = 141:150, y = ints$upper, col = "red")
}
